import { LoginForm } from '@/app/(public)/(modules)/login/components/molecules'

const LoginView = () => {
  return (
    <div className="flex items-center justify-center h-full">
      <LoginForm />
    </div>
  )
}

export default LoginView
